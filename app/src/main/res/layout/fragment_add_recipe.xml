<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        tools:context=".NewIngredientFragment"
        android:padding="12dp">

        <TextView
            android:id="@+id/addRecipeHeading"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentTop="true"
            android:layout_marginTop="16dp"
            android:text="Add recipe"
            android:textStyle="bold"
            android:textColor="@color/red"
            android:textSize="24sp" />

        <EditText
            android:id="@+id/addRecipeName"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:hint="Enter the name"
            android:padding="12dp"
            android:textSize="20sp"
            android:layout_below="@id/addRecipeHeading"
            android:layout_marginTop="16dp"
            android:maxLines="1"/>

        <EditText
            android:id="@+id/addRecipePrepTime"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Preparation time in minutes"
            android:gravity="top"
            android:padding="12dp"
            android:textSize="18sp"
            android:layout_below="@id/addRecipeName"
            android:layout_marginTop="12dp" />

        <EditText
            android:id="@+id/addRecipeCookingTime"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Cooking time in minutes"
            android:gravity="top"
            android:padding="12dp"
            android:textSize="18sp"
            android:layout_below="@id/addRecipePrepTime"
            android:layout_marginTop="12dp" />

        <EditText
            android:id="@+id/addRecipeDesc"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:hint="Enter the description"
            android:gravity="top"
            android:padding="12dp"
            android:textSize="18sp"
            android:layout_below="@id/addRecipeCookingTime"
            android:layout_marginTop="12dp" />
    </RelativeLayout>
</layout>